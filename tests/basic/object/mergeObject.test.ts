import { test, expect } from 'vitest';
import { mergeObject } from '../../../packages/basic/object/mergeObject';

test('../../../packages/basic/object/mergeObject', () => {
        expect(mergeObject( )).toBe( );
 });