import { test, expect } from 'vitest';
import { isObject } from '../../../packages/basic/object/isObject';

test('../../../packages/basic/object/isObject', () => {
        expect(isObject( )).toBe( );
 });