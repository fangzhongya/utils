import { test, expect } from 'vitest';
import { toggleArray } from '../../../packages/basic/array/toggleArray';

test('../../../packages/basic/array/toggleArray', () => {
        expect(toggleArray( )).toBe( );
 });